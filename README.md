# Luxe Studio

A premium design collaboration workspace with local layout generation, brand kit controls, and a polished review canvas.

## Run locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Start the app:
   `npm run dev`
3. Open the local preview at http://localhost:3000
